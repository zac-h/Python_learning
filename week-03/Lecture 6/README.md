#CS50P Python Fundamentals

# Topics covered
- File I/O
- Reading and writing from CSV files
- Open and With functions
- reader, DictReader and writer, DictWriter functions
- PILLOW library

# Exercises
- Use sys.argv to input file name as string from command line prompt
- Handle exceptions such as missing file name, too few or too many command line inputs, wrong command line input type, wrong file type or file not found and raise a ValueError upon identifying
- Check file line by line filtering for blank of commented lines and counting number of actual lines of code
- Read in csv file of menu and generate contents as tabulated ASCII art in the terminal
- Take input from existing CSV file where first name and last name are entwined in "last, first" and separate names into separate columns in a new CSV file
- Overlay one image on another and export as merged image file